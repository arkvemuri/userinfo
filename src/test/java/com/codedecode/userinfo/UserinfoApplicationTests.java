package com.codedecode.userinfo;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class UserinfoApplicationTests {

    @Test
    void contextLoads() {
        /*
         * This test method is intentionally empty as it only validates that the Spring context loads successfully.
         * The @SpringBootTest annotation triggers the full application context to be created.
         * If any beans or configurations are misconfigured, this test will fail during context initialization.
         * No additional assertions are needed as the test's purpose is to verify proper Spring Boot startup.
         */
    }

}
